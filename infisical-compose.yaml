servvices:
  backend:
    container_name: infisical-backend
    restart: unless-stopped
    image: infisical/infisical:latest-postgres
    pull_policy: always
    ports:
      - 80:8080
    environment:
      - NODE_ENV=production
    networks:
      - infisical

  networks:
    infisical:
